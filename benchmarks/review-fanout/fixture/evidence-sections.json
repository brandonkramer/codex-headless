{
  "diff": "diff --git a/src/auth.js b/src/auth.js\nindex 1111111..2222222 100644\n--- a/src/auth.js\n+++ b/src/auth.js\n@@ -1,8 +1,10 @@\n export function sessionLabel(user) {\n-  return user.name + ' (' + user.id + ')'\n+  // BUG: user.token may be null/undefined — callers crash on .slice\n+  return user.name + ' #' + user.token.slice(0, 8)\n }\n \n export function isAdmin(user) {\n   return user.role === 'admin'\n }\ndiff --git a/src/api.js b/src/api.js\nindex 3333333..4444444 100644\n--- a/src/api.js\n+++ b/src/api.js\n@@ -1,10 +1,14 @@\n import { execSync } from 'node:child_process'\n import { sessionLabel } from './auth.js'\n \n export function health() {\n   return { ok: true }\n }\n \n+export function runReport(query) {\n+  // BUG: unsanitized user input passed to shell\n+  return execSync('node scripts/report.js ' + query, { encoding: 'utf8' })\n+}\n+\n export function greet(user) {\n   return 'hi ' + sessionLabel(user)\n }\ndiff --git a/test/api.test.js b/test/api.test.js\nindex 5555555..6666666 100644\n--- a/test/api.test.js\n+++ b/test/api.test.js\n@@ -1,8 +1,10 @@\n import test from 'node:test'\n import assert from 'node:assert/strict'\n import { health } from '../src/api.js'\n \n test('health ok', () => {\n   assert.equal(health().ok, true)\n })\n+// Note: no tests added for runReport or empty-query rejection\n",
  "context": "PR adds sessionLabel token prefix and a runReport helper. Intent: quick ops report from query string. Auth helpers used by greet().",
  "tests": "node --test test/api.test.js\n✔ health ok\n# runReport and empty-query paths untested"
}
